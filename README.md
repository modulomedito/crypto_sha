# crypto_sha

SHA (Secure Hash Algorithm) for embedded MCU

## Test

1. Open terminal, `cd` into the project root
2. Run test in the terminal by typing `xmake test`
3. Or run test with verbose messages `xmake test -v`
